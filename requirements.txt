# Django framework
Django==4.2.1

# WSGI server for deployment
gunicorn==22.0.0

# PostgreSQL database driver (pre-built binary)
psycopg2-binary==2.9.9

# Image handling library
Pillow==10.1.0

# Environment variables
python-dotenv==1.0.0

# Static file serving in production
whitenoise==6.5.0

# Optional: forms handling
django-crispy-forms==2.0.3

# Optional: if you use Django storages for S3 or similar
boto3==1.28.0

# Make sure pip, setuptools, and wheel are up to date
pip==25.3
setuptools==68.0.0
wheel==0.41.0
